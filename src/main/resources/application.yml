spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ray_school_db
    username: dev-user
    password: 12345678*
  jpa:
    hibernate:
      ddl-auto: update
    properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  jackson:
    serialization:
      fail-on-empty-beans: false
  liquibase:
        change-log: classpath:db/changelog/db.changelog-master.yaml
        enabled: true
  mail:
    host: smtp.gmail.com
    username: mymotherhang@gmail.com
    password: uybvnzvretrgwuyq
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
              enable: true
  mvc:
      pathmatch:
        matching-strategy: ant_path_matcher